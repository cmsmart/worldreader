<h1>New Book</h1>

<% if current_user.admin? %>
    <%= render 'form', book: @book %>
<% elsif current_user.publisher? %>
    <%= render 'form_publisher', book: @book %>   
<% end %>

<%= link_to 'Back', books_path %>
