<h1>Regions</h1>

<div class="row">
    <% @regions.each do |region| %>
        <div class="col-sm-3"><%= link_to region.name, region %></div>
        <%#= link_to 'Show', region %>
        <%#= link_to 'Edit', edit_region_path(region) %>
    <% end %>
</div>
