<% if current_user.read?(book) %>
    <%= link_to "remove", unread_path(book_id: book.id), remote: true, method: :delete, class: 'button' %>
<% else %>
    <%= link_to "I Have read this", read_path(book_id: book.id), remote: true, method: :post, class: 'button' %>
<% end %>